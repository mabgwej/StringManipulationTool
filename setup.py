from setuptools import setup, find_packages

setup(
    name='StringManipulationTool',
    version='0.1.0',  # Update this to reflect the current version
    packages=find_packages(),
    description='A tool for manipulating and analyzing strings.',
    author='Your Name',
    author_email='your.email@example.com',
    license='MIT',
    install_requires=[],  # List dependencies here if any
)
